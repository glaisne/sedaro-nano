apiVersion: v1
kind: Service
metadata:
  name: {{ include "sedaro-nano.fullname" . }}
  labels:
    {{- include "sedaro-nano.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sedaro-nano.selectorLabels" . | nindent 4 }}
