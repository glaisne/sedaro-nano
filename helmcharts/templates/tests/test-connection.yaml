apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sedaro-nano.fullname" . }}-test-connection"
  labels:
    {{- include "sedaro-nano.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sedaro-nano.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
